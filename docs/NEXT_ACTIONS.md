# Next Actions

> 更新日期：2026-07-06
> 本文件只保留当前可执行任务队列。早期 TASK-002 至 TASK-012、旧 Phase 路线和已完成的大量 Round 填充日志已从正文移除；需要追溯时查看 git history。

## 当前推进规则

- 默认直接在 `main` 上修改、验证、commit、push。
- 每轮只做一个明确任务。
- 用户当轮直接指定任务时，以用户指令优先。
- 涉及考试、考纲、院校信息时，必须核验最新官方源。
- 涉及真实 VPS 写操作时，必须先取得用户明确授权。

## TASK-RR-56：治理规则改为直接推 main，并压缩旧路线文档

- 状态：**done**（2026-07-06）
- 背景：用户指出“禁止直接推送 main”的仓库规则不符合当前单人学习仓库协作方式；早期路线图过于陈旧，继续保留长篇旧 Phase/TASK 内容会误导后续 Agent。
- 目标：
  - 将 `AGENTS.md`、`docs/AUTO_ADVANCE_PROTOCOL.md`、治理文档、checklist、prompt 和 `agent_gate` 改为默认直接推 `main`。
  - 将 `docs/CODEX_LONG_TERM_PLAN.md`、`docs/PROJECT_STATE.md`、`docs/NEXT_ACTIONS.md` 从历史日志型文档压缩为当前事实型文档。
  - 同步 README 中与 Git 流程、当前阶段、Web UI 启动端口相关的用户入口说明。
  - 修正 `agent_gate` 任务标题解析，并让 `branch_hint` 输出为 `main`。
- 验收：
  - 当前规则文档不再要求“禁止直接推 main”或“必须独立分支”。
  - `python3 scripts/agent_gate.py --json --no-require-clean` 输出 `branch_hint: "main"`。
  - 标准验证命令通过。
- 是否需要用户介入：否。用户已明确授权直接改规则、直接推送，并说明当前不存在需要保护的真实学习记录。

## TASK-RR-57：第三轮项目用户视角评测与入口清理

- 状态：**done**（2026-07-06）
- 背景：用户要求持续对整个项目做用户视角评测，累计 5 个问题后写报告并修复。治理规则压缩后，需要继续评测 README、路线文档和 Web UI 是否清晰、好用、美观。
- 目标：
  - 新增第三轮评测报告。
  - 调整 README 入口顺序与当前状态描述，降低工具配置对新用户的干扰。
  - 修正 `STAGE_PLAN.md` 中已过期的 Stage 0 / Round 展开状态。
  - 修复 Web UI 中当前阅读任务与终端不联动的问题。
  - 压缩移动端顶部 chrome，让学习工作区更早出现。
- 验收：
  - 新报告列出 5 个新问题。
  - 桌面 `progress.html?round=round_02` 当前阅读任务可直接绑定终端。
  - 390px 移动端无横向溢出，学习工作区更早进入首屏。
  - 标准验证命令通过。
- 是否需要用户介入：否。

## TASK-RR-58：第四轮项目用户视角评测

- 状态：**done**（2026-07-06）
- 背景：用户要求持续重复“评测累计 5 个问题 → 写报告 → 修复 → 推 main”的循环，直到找不出新的问题。
- 目标：继续从普通学习者视角评测 README、路线文档、Web UI 桌面/移动端、任务流和视觉层级；累计 5 个新问题后停止评测、生成报告并修复。
- 验收：
  - 新增第四轮评测报告并列出 5 个新问题。
  - README UI 启动说明使用真实本地 API 服务。
  - 默认 Web UI 当前任务、终端提示和 Round 浏览保持一致。
  - 软考计划入口和错题系统文档不再出现过期或违规引导。
  - 标准验证命令通过，并直接 push `main`。
- 是否需要用户介入：否。

## TASK-RR-59：第五轮项目用户视角评测

- 状态：**done**（2026-07-06）
- 背景：第四轮已修复默认任务、终端提示、软考计划入口和错题系统规则；用户要求继续重复评测和修复循环，直到找不出新的问题。
- 目标：继续从普通学习者视角评测 README、路线文档、Web UI 桌面/移动端、任务流、记录/存档/复盘路径和视觉层级；累计 5 个新问题后停止评测、生成报告并修复。
- 验收：
  - 新增第五轮评测报告并列出 5 个新问题。
  - 剩余静态服务器 workflow 文档改为真实本地 API 服务。
  - 默认非工程任务不再显示大块空闲终端面板。
  - 完成任务必须先进入记录流程并填写本次记录。
  - 侧栏 `进度设置` 能直接展开管理区。
  - 标准验证命令通过，并直接 push `main`。
- 是否需要用户介入：否。

## TASK-RR-60：第六轮项目用户视角评测

- 状态：**done**（2026-07-06）
- 背景：第五轮已修复默认文案、空闲终端占位、记录完成流程、侧栏设置导航和残留静态服务器文档；用户要求继续重复评测和修复循环，直到找不出新的问题。
- 目标：继续从普通学习者视角评测 README、路线文档、Web UI 桌面/移动端、记录/存档/复盘路径、软考启动材料和视觉层级；累计 5 个新问题后停止评测、生成报告并修复。
- 验收：
  - 新增第六轮评测报告并列出 5 个新问题。
  - Web UI 的跨主线任务区改为任务清单视角。
  - 未完成任务只保留 `记录并完成`，不再同时显示重复的 `记录` 入口。
  - 运行结果提示、PROJECT_STATE 数据来源说明、软考启动 README 和阅读器相对链接支持已同步当前事实。
  - 标准验证命令通过，并直接 push `main`。
- 是否需要用户介入：否。

## TASK-RR-61：第七轮项目用户视角评测

- 状态：**done**（2026-07-06）
- 背景：第六轮已修复任务清单命名、未完成任务重复记录入口、旧完成提示、状态文档数据来源和软考启动页链接；用户要求继续重复评测和修复循环，直到找不出新的问题。
- 目标：继续从普通学习者视角评测 README、路线文档、Web UI 桌面/移动端、记录/存档/复盘路径、软考启动材料和视觉层级；累计 5 个新问题后停止评测、生成报告并修复。
- 验收：
  - 新增第七轮评测报告并列出 5 个新问题。
  - 完成流程文案、记录弹窗示例和终端路径显示已与真实交互一致。
  - Stage 2 软考入口状态已同步当前事实。
  - 软考 / 数学二 / 408 启动页面已补最小产物清单。
  - 标准验证命令通过，并直接 push `main`。
- 是否需要用户介入：否。

## TASK-RR-62：第八轮项目用户视角评测

- 状态：**done**（2026-07-06）
- 背景：第七轮已修复完成流程文案、记录弹窗示例、终端路径一致性、Stage 2 过期描述和启动页最小产物清单；用户要求继续重复评测和修复循环，直到找不出新的问题。
- 目标：继续从普通学习者视角评测 README、路线文档、Web UI 桌面/移动端、记录/存档/复盘路径、启动材料、任务列表和视觉层级；累计 5 个新问题后停止评测、生成报告并修复。
- 验收：
  - 已新增第八轮评测报告并列出 5 个新问题。
  - CLI 无参数输出显示任务标题和所属分组。
  - Web UI Stage 0、当前关注项和工程终端默认绑定状态已同步当前事实。
  - 标准验证命令通过，并直接 push `main`。
- 是否需要用户介入：否。

## TASK-RR-63：第九轮项目用户视角评测

- 状态：**done**（2026-07-06）
- 背景：第八轮已修复 CLI 可读性、Stage 0 状态、当前关注项规则、工程终端默认绑定和 README 中 Web UI / CLI 分工说明；用户要求继续重复评测和修复循环，直到找不出新的问题。
- 目标：继续从普通学习者视角评测 README、路线文档、Web UI 桌面/移动端、CLI、任务列表、记录/存档/复盘路径和视觉层级；累计 5 个新问题后停止评测、生成报告并修复。
- 验收：
  - 已新增第九轮评测报告并列出 5 个新问题。
  - CLI 默认状态输出已压缩，并支持 `--all`、`--lane`、`--limit`。
  - Web UI 终端文案、快捷命令和阶段参考面板已同步当前事实。
  - 标准验证命令通过，并直接 push `main`。
- 是否需要用户介入：否。

## TASK-RR-64：第十轮项目用户视角评测

- 状态：**done**（2026-07-06）
- 背景：第九轮已修复 CLI 输出墙、状态过滤、终端自动绑定文案、终端快捷命令和 Stage 近似进度误读；用户要求继续重复评测和修复循环，直到找不出新的问题。
- 目标：继续从普通学习者视角评测 README、路线文档、Web UI 桌面/移动端、CLI、任务列表、记录/存档/复盘路径和视觉层级；累计 5 个新问题后停止评测、生成报告并修复。
- 验收：
  - 已新增第十轮评测报告并列出 5 个新问题。
  - 软考 / 数学二计划文档和 Stage 3 模考路径已同步当前事实。
  - Web UI 生成的存档快照和终端历史不再污染 Git 状态。
  - 标准验证命令通过，并直接 push `main`。
- 是否需要用户介入：否。

## TASK-RR-65：第十一轮项目用户视角评测

- 状态：**done**（2026-07-06）
- 背景：第十轮已修复计划文档编号、数学二启动骨架事实、Stage 3 模考路径以及 Web UI 本地生成记录的 Git 噪音；用户要求继续重复评测和修复循环，直到找不出新的问题。
- 目标：继续从普通学习者视角评测 README、路线文档、Web UI 桌面/移动端、CLI、任务列表、记录/存档/复盘路径和视觉层级；累计 5 个新问题后停止评测、生成报告并修复。
- 验收：
  - 已新增第十一轮评测报告并列出 5 个新问题。
  - 管理区闭合状态、终端自动联动文案、任务清单按钮文案和终端记录说明已同步当前事实。
  - 标准验证命令通过，并直接 push `main`。
- 是否需要用户介入：否。

## TASK-RR-66：第十二轮项目用户视角评测

- 状态：**done**（2026-07-06）
- 背景：第十一轮已修复管理区未真正收起、终端自动联动旧文案、任务清单按钮文案不一致和终端记录 README 旧流程；用户要求继续重复评测和修复循环，直到找不出新的问题。
- 目标：继续从普通学习者视角评测 README、路线文档、Web UI 桌面/移动端、CLI、任务列表、记录/存档/复盘路径和视觉层级；累计 5 个新问题后停止评测、生成报告并修复。
- 验收：
  - 已新增第十二轮评测报告并列出 5 个新问题。
  - README、Round 00 指引、Round 00–21 入口 URL、Round 学习提示按钮名和 ADR / 自动推进文档已同步当前事实。
  - 标准验证命令通过，并直接 push `main`。
- 是否需要用户介入：否。

## TASK-RR-67：第十三轮项目用户视角评测

- 状态：**done**（2026-07-06）
- 背景：第十二轮已修复 Round 00 完成误读、旧静态入口、旧端口、Round 指引按钮名和早期架构描述；用户要求继续重复评测和修复循环，直到找不出新的问题。
- 目标：继续从普通学习者视角评测 README、路线文档、Web UI 桌面/移动端、CLI、任务列表、记录/存档/复盘路径和视觉层级；累计 5 个新问题后停止评测、生成报告并修复。
- 验收：
  - 已新增第十三轮评测报告并列出 5 个新问题。
  - Web UI、CLI、Round 01 / 02 / 12 指引中的完成/终端文案已对齐 `终端练习` 与 `记录并完成`。
  - 演示动作日志已清空并重新生成反馈 JSON，空进度不再显示历史撤销动作。
  - 标准验证命令通过，并直接 push `main`。
- 是否需要用户介入：否。

## TASK-RR-68：第十四轮项目用户视角评测

- 状态：**done**（2026-07-06）
- 背景：第十三轮已修复终端命名、完成记录文案、Round 01 / 02 / 12 遗留手动标记提示和演示动作日志污染；用户要求继续重复评测和修复循环，直到找不出新的问题。
- 目标：继续从普通学习者视角评测 README、路线文档、Web UI 桌面/移动端、CLI、任务列表、记录/存档/复盘路径和视觉层级；累计 5 个新问题后停止评测、生成报告并修复。
- 验收：
  - 已新增第十四轮评测报告并列出 5 个新问题。
  - 进度规则、README、Linux 计划、软考 P0 骨架和 408 模块骨架已同步当前事实。
  - 标准验证命令通过，并直接 push `main`。
- 是否需要用户介入：否。

## TASK-RR-69：第十五轮项目用户视角评测

- 状态：**done**（2026-07-06）
- 背景：第十四轮已修复进度规则旧占位描述、Round 12–21 目录误读、Linux 计划旧入口、软考 composition/network/se 启动骨架和 408 四模块启动骨架；用户要求继续重复评测和修复循环，直到找不出新的问题。
- 目标：继续从普通学习者视角评测 README、路线文档、Web UI 桌面/移动端、CLI、任务列表、记录/存档/复盘路径和视觉层级；累计 5 个新问题后停止评测、生成报告并修复。
- 验收：
  - 已新增第十五轮评测报告并列出 5 个新问题。
  - soft_exam 模块骨架、Web UI 任务注册、Stage Plan、Knowledge Mapping 和反馈 JSON 已同步当前事实。
  - 标准验证命令通过，并直接 push `main`。
- 是否需要用户介入：否。

## TASK-RR-70：第十六轮项目用户视角评测

- 状态：**pending**
- 背景：第十五轮已补齐软考 12 个模块启动骨架并注册为 Web UI 任务；用户要求继续重复评测和修复循环，直到找不出新的问题。
- 目标：继续从普通学习者视角评测 README、路线文档、Web UI 桌面/移动端、CLI、任务列表、记录/存档/复盘路径和视觉层级；累计 5 个新问题后停止评测、生成报告并修复。
- 验收：
  - 若找到 5 个新问题，新增第十六轮评测报告并完成修复。
  - 若找不到新问题，必须给出覆盖范围和证据，再判断目标是否接近完成。
  - 标准验证命令通过，并直接 push `main`。
- 是否需要用户介入：否。

## 推荐下一步

1. 继续做 Web UI / README / 路线文档第十六轮用户视角评测。
2. 从软考主线选择 1-2 个模块开始写概念地图；具体考纲仍以最新官方源为准。
3. 如要启动 VPS 支线，先按 `docs/templates/remote_operation_confirmation.md` 取得授权，再记录真实操作结果。
