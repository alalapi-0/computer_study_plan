# Round 12 · Week 3 笔记（日志轮转与定时入口）

## 本周目标

- 生成 `log_utils.py` 骨架，含 `RotatingFileHandler` 示例。
- 生成 `scripts/run_batch.sh` 作为 cron 可挂接入口（仅骨架）。

## 本周自查

- [ ] 能说明日志轮转防止磁盘占满的原因
- [ ] 能写出一条 crontab 示例（不必真写入系统 crontab）
