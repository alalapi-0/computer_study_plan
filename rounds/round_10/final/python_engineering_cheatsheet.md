# Round 10 · Python 工程化速查

## 建议顺序

1. 先拆 `cli.py` / `core.py` / `io_utils.py`。
2. 再补 `config.ini` 与 `log_utils.py`。
3. 最后补错误类型与收口检查。

## 最小通过标准

- 沙盒项目不再依赖单文件堆叠全部逻辑。
- 配置与日志职责分离，用户输出与排查日志可区分。
- 核心纯函数具备最小自检或可断言样例。
